# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Base44 app link (contains the app id — local machine config, not shared)
base44/.app.jsonc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Heavy local frame sequences (~2GB) — kept on disk, delivered to prod separately, never in git
public/frames/
